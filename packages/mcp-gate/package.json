{
  "name": "@agentgate/mcp-gate",
  "version": "0.0.1",
  "description": "MCP-server gate — wraps any MCP server and routes tool calls through agentgate",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "agentgate-mcp-gate": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --import tsx --test test/*.test.ts",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "@agentgate/sdk": "^0.0.1"
  },
  "devDependencies": {
    "@types/node": "^22.9.0"
  }
}
