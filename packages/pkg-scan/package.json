{
  "name": "@agentgate/pkg-scan",
  "version": "0.0.1",
  "description": "Pre-install supply-chain scanner. Returns risk + signals for npm/PyPI packages without installing them.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^22.9.0"
  }
}
