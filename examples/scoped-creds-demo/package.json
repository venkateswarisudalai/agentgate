{
  "name": "@agentgate/example-scoped-creds-demo",
  "version": "0.0.1",
  "private": true,
  "description": "Demo: just-in-time scoped credentials. Approval -> short-lived token bound to (agent, action, scope, expiry). Token can't be replayed, can't escalate scope, can't outlive its window.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "tsc -p tsconfig.json && node dist/index.js"
  },
  "dependencies": {
    "@agentgate/sdk": "*"
  },
  "devDependencies": {
    "@types/node": "^22.9.0"
  }
}
