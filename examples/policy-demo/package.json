{
  "name": "@agentgate/example-policy-demo",
  "version": "0.0.1",
  "private": true,
  "description": "Demo: declarative policies that auto-allow / auto-deny / route to human",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "tsc -p tsconfig.json && node dist/index.js"
  },
  "dependencies": {
    "@agentgate/sdk": "*"
  },
  "devDependencies": {
    "@types/node": "^22.9.0"
  }
}
