{
  "name": "@agentgate/example-mcp-demo",
  "version": "0.0.1",
  "private": true,
  "description": "Demo: drive a real MCP client through mcp-gate — one safe tool call passes through, one dangerous call freezes for approval. Zero agent code.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js"
  },
  "devDependencies": {
    "@types/node": "^22.9.0"
  }
}
